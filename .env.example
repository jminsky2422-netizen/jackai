# Copy to `.env` and set your key.
# Local: `npm run dev` (needs Vercel CLI) or `npm run dev:local` (Node only, reads this file).
# Vercel: add OPENAI_API_KEY in Project → Settings → Environment Variables.
OPENAI_API_KEY=
